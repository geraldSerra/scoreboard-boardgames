import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.innmotrix.scoreboard',
  appName: 'Carcassonne Scoreboard',
  webDir: 'build'
};

export default config;
